line-length = 100
extend-exclude = ["decode.ipynb"]

[lint]
select = ["E", "F", "I"]  # pycodestyle errors, pyflakes, isort

[lint.isort]
known-first-party = ["api", "worker", "serving", "config", "db", "gpu_lock", "storage", "sqs", "watchdog"]
